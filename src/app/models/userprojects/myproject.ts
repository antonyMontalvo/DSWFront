export interface myProjectI { // Usado para guardar cada data de la response en un array.
    link_video: string,
    collaborators: any,
    _id: string,
    title: string,
    short_desc: string,
    category: string,
    ubication: string,
    monetary_goal: number,
    start_date: Date,
    end_date: Date,
    challenges: string,
    reward: any,
    long_desc: any,
    sponsors: any,
    __v: number
}